rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /scan-images/{userId}/{year}/{month}/{fileName} {
      allow read: if request.auth != null && request.auth.uid == userId;
      allow write: if false;
    }
  }
}
